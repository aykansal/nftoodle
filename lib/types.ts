export interface nft {
    blockchain: string;
    chain_id: number;
    contract_address: string;
    external_url: string;
    image_url: string;
    marketplaces: string;
}

export interface tokenList {
    image_url: string;
}